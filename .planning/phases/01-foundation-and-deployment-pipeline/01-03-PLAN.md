---
phase: 01-foundation-and-deployment-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified: []
autonomous: false
requirements:
  - SITE-01
tags:
  - deployment
  - github-pages
  - verification

user_setup:
  - service: github
    why: "GitHub Pages must be enabled with 'GitHub Actions' as source before the first deploy can succeed"
    dashboard_config:
      - task: "Set Pages source to GitHub Actions"
        location: "https://github.com/neoasaad/neoasaad.github.io/settings/pages → Build and deployment → Source → 'GitHub Actions'"

must_haves:
  truths:
    - "Local git repo has a remote named `origin` pointing to `git@github.com:neoasaad/neoasaad.github.io.git` or the HTTPS equivalent"
    - "All Phase 1 files (scaffold + workflow) are committed to `main`"
    - "GitHub Pages source is set to 'GitHub Actions' in repo Settings"
    - "Pushing to `main` triggers the workflow and the deploy job completes successfully"
    - "https://neoasaad.github.io loads the placeholder page (HTTP 200, contains 'Site coming soon.')"
    - "Browser DevTools network tab shows zero 404 responses for assets on the live page"
  artifacts: []
  key_links:
    - from: "local git repository"
      to: "github.com/neoasaad/neoasaad.github.io"
      via: "git remote origin pushing main branch"
      pattern: "neoasaad/neoasaad\\.github\\.io"
    - from: "push to main"
      to: "live URL https://neoasaad.github.io"
      via: ".github/workflows/deploy.yml → withastro/action@v6 → actions/deploy-pages@v5"
      pattern: "github-pages environment"
---

<objective>
Wire the local repo to GitHub, push Phase 1 to `main`, configure the GitHub Pages source, and verify the placeholder page is live at https://neoasaad.github.io. This is the final plan in Phase 1 and proves end-to-end that SITE-01 is satisfied.

Purpose: Validates the full pipeline before any UI is built. After this plan completes, Phase 2 can build directly on a working deploy loop (push → build → deploy → live) without infrastructure surprises.

Output:
- Configured `git remote origin` pointing to `neoasaad/neoasaad.github.io`
- All Phase 1 files committed and pushed to `main`
- GitHub Pages source confirmed as "GitHub Actions"
- First successful deploy run on the Actions tab
- Live URL https://neoasaad.github.io returns HTTP 200 with the placeholder page

Note: This plan contains TWO checkpoints because the work spans actions Claude can automate (git remote, push, curl) and one manual step Claude truly cannot do (clicking the radio button in GitHub repo Settings → Pages).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-RESEARCH.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-PATTERNS.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-VALIDATION.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure git remote and verify target repo state</name>
  <files></files>
  <read_first>
    - /Users/neoasaad/Projects/Asaad_Webpage/.planning/phases/01-foundation-and-deployment-pipeline/01-RESEARCH.md (Pitfall #4 — wrong repo would deploy to /Asaad_Webpage URL; Open Questions #1 — confirm neoasaad/neoasaad.github.io exists and is empty)
    - /Users/neoasaad/Projects/Asaad_Webpage/CLAUDE.md (Key Configuration Notes — must use neoasaad/neoasaad.github.io user page repo)
    - Run `git -C /Users/neoasaad/Projects/Asaad_Webpage remote -v` to see current remote state (expected: empty)
    - Run `git -C /Users/neoasaad/Projects/Asaad_Webpage branch --show-current` to confirm current branch is `main`
  </read_first>
  <action>
    Step 1 — Verify the target repo exists on GitHub:
    ```
    curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/neoasaad/neoasaad.github.io
    ```
    Expected: `200`. If `404`, STOP — the target repo does not exist; surface a checkpoint asking the user to create it (this should not happen per RESEARCH.md verification, but check anyway).

    Step 2 — Check whether the remote repo `main` branch already has content (per RESEARCH.md Open Questions #1):
    ```
    curl -s https://api.github.com/repos/neoasaad/neoasaad.github.io/branches/main | grep -q '"name": "main"' && echo "main exists on remote" || echo "main is empty on remote"
    ```

    Step 3 — Add the remote and verify:
    ```
    cd /Users/neoasaad/Projects/Asaad_Webpage
    git remote add origin git@github.com:neoasaad/neoasaad.github.io.git
    git remote -v
    ```
    If `git remote add origin` fails because origin already exists, run `git remote set-url origin git@github.com:neoasaad/neoasaad.github.io.git` instead.

    The remote URL MUST contain `neoasaad/neoasaad.github.io.git` and MUST NOT contain `Asaad_Webpage` (per RESEARCH.md Pitfall #4 — wrong repo would deploy to /Asaad_Webpage URL and break asset paths).

    Step 4 — Decision point:
    - If remote `main` is empty (Step 2 returned "main is empty on remote"): proceed to Task 2 (normal push)
    - If remote `main` HAS content (Step 2 returned "main exists on remote"): emit a checkpoint asking the user whether to (a) force-push and overwrite, (b) pull/rebase first, or (c) abort. Do NOT auto-force-push.

    Step 5 — Sanity check the local working tree before commit/push:
    ```
    cd /Users/neoasaad/Projects/Asaad_Webpage
    git status
    ls -la .github/workflows/deploy.yml astro.config.mjs src/layouts/Layout.astro src/pages/index.astro src/styles/global.css package.json pnpm-lock.yaml
    ```
    All files from Plans 01 and 02 must exist locally.
  </action>
  <verify>
    <automated>cd /Users/neoasaad/Projects/Asaad_Webpage && git remote -v | grep -E '^origin\s+.*neoasaad/neoasaad\.github\.io(\.git)?\s+\(push\)$' && ! (git remote -v | grep -q 'Asaad_Webpage') && test -f astro.config.mjs && test -f .github/workflows/deploy.yml && test -f package.json && test -f pnpm-lock.yaml && curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/neoasaad/neoasaad.github.io | grep -q '^200$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /Users/neoasaad/Projects/Asaad_Webpage remote -v` shows `origin` with URL containing `neoasaad/neoasaad.github.io`
    - `git -C /Users/neoasaad/Projects/Asaad_Webpage remote -v | grep -c Asaad_Webpage` returns `0` (per RESEARCH.md Pitfall #4)
    - `curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/neoasaad/neoasaad.github.io` returns `200`
    - All Plan 01 and Plan 02 files exist locally: astro.config.mjs, src/layouts/Layout.astro, src/pages/index.astro, src/styles/global.css, package.json, pnpm-lock.yaml, .github/workflows/deploy.yml
    - If remote `main` had pre-existing content, a checkpoint was raised before any push action
  </acceptance_criteria>
  <done>Git remote `origin` points to neoasaad/neoasaad.github.io, the repo is confirmed to exist on GitHub, all Phase 1 files are present in the working tree, and any pre-existing remote content has been escalated to the user before push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human enables GitHub Pages source = "GitHub Actions"</name>
  <what-built>Plan 01 produced the Astro scaffold + placeholder page. Plan 02 produced .github/workflows/deploy.yml. Task 1 configured the git remote. Everything is ready to push, EXCEPT the GitHub Pages source must be set to "GitHub Actions" in repo Settings before the first push — otherwise the deploy job fails with "github-pages environment not found" (RESEARCH.md Pitfall #1).</what-built>
  <how-to-verify>
    Manual step required (GitHub web UI only — no CLI/API equivalent that bypasses the UI permission flow for first-time setup):

    1. Open https://github.com/neoasaad/neoasaad.github.io/settings/pages in a browser
    2. Under "Build and deployment", find "Source"
    3. Select "GitHub Actions" from the dropdown (NOT "Deploy from a branch")
    4. Confirm the page now shows "GitHub Actions" as the active source

    Then reply with one of:
    - `approved` — Pages source is set to GitHub Actions, ready to push
    - `repo not found` — neoasaad.github.io repo does not exist (we'll create it first)
    - `already had content` — remote main already had content; need to decide overwrite vs pull
    - any other comment — describes a problem
  </how-to-verify>
  <acceptance_criteria>
    - User confirms in chat that GitHub Pages → Source is set to "GitHub Actions"
    - User has not raised a blocking issue (repo missing, content conflict, etc.)
  </acceptance_criteria>
  <resume-signal>Type "approved" once GitHub Pages source is set to GitHub Actions, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit Phase 1 scaffold + workflow, push to main, observe deploy</name>
  <files></files>
  <read_first>
    - /Users/neoasaad/Projects/Asaad_Webpage/.planning/phases/01-foundation-and-deployment-pipeline/01-RESEARCH.md (Pattern 2 + Pitfalls #1, #3, #4 — all preconditions for first push)
    - Read current git status: `git -C /Users/neoasaad/Projects/Asaad_Webpage status`
    - Read existing commits: `git -C /Users/neoasaad/Projects/Asaad_Webpage log --oneline -10`
  </read_first>
  <action>
    Step 1 — Stage and commit Phase 1 files. Use explicit file paths (not `git add -A`) per global rules:

    ```
    cd /Users/neoasaad/Projects/Asaad_Webpage
    git add package.json pnpm-lock.yaml tsconfig.json astro.config.mjs .gitignore \
            src/layouts/Layout.astro src/pages/index.astro src/styles/global.css \
            public/favicon.svg .github/workflows/deploy.yml
    git status
    ```

    Verify ONLY the intended files are staged (no `node_modules/`, no `dist/`, no `.astro/`).

    Step 2 — Commit:
    ```
    git commit -m "$(cat <<'EOF'
    feat(01): scaffold astro 6 site and github pages deploy pipeline

    - Astro 6 minimal scaffold with strict TypeScript
    - astro.config.mjs: site=https://neoasaad.github.io, output=static, fonts via fontProviders.fontsource()
      (Inter + Playfair Display); NO base property (user page repo)
    - Layout.astro wires <Font /> components and imports global.css design tokens
    - Placeholder index.astro proves end-to-end build before UI work begins (Phase 2)
    - .github/workflows/deploy.yml uses withastro/action@v6 + actions/deploy-pages@v5
      with concurrency control and workflow_dispatch trigger

    Closes Phase 1 plans 01 and 02. SITE-01 build + CI portion satisfied.

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```

    Step 3 — Push to main:
    ```
    git push -u origin main
    ```

    If the push is rejected (non-fast-forward) and the user previously approved overwriting in Task 2, force-push only after explicit confirmation in chat. Otherwise abort and surface the conflict.

    Step 4 — Observe the workflow run. Use `gh` CLI:
    ```
    gh run list --workflow=deploy.yml --repo neoasaad/neoasaad.github.io --limit 1
    gh run watch --repo neoasaad/neoasaad.github.io
    ```
    The deploy must complete with `conclusion: success`. If `gh` is not authenticated, fall back to: surface a checkpoint asking the user to open https://github.com/neoasaad/neoasaad.github.io/actions and confirm the run succeeded.

    Step 5 — Verify the live URL:
    ```
    sleep 30   # allow GitHub Pages CDN propagation
    curl -s -o /tmp/asaad-live.html -w "HTTP %{http_code}\n" https://neoasaad.github.io
    grep -c 'Site coming soon' /tmp/asaad-live.html
    ```
    Expected: `HTTP 200` and `grep` returns at least `1`. If the URL returns 404, wait another 60s and retry once (CDN propagation).

    Step 6 — Spot-check for asset 404s. Pull the live HTML, extract asset paths, and HEAD each:
    ```
    curl -s https://neoasaad.github.io | grep -oE '(href|src)="[^"]*\.(css|js|woff2?|svg|png|ico)"' | sed -E 's/^(href|src)="//; s/"$//' | while read p; do
      [ "${p:0:1}" = "/" ] && url="https://neoasaad.github.io${p}" || url="${p}"
      code=$(curl -s -o /dev/null -w "%{http_code}" "${url}")
      echo "${code} ${url}"
    done
    ```
    All asset HEAD responses MUST be 200 (or 304). Any 404 fails this task.

    Critical: If ANY asset URL contains `/Asaad_Webpage/`, that confirms the dreaded base-path bug — STOP, surface to user immediately. Per RESEARCH.md, this should not happen because we have NO `base` in astro.config.mjs.
  </action>
  <verify>
    <automated>cd /Users/neoasaad/Projects/Asaad_Webpage && git log --oneline -1 | grep -q 'feat(01)' && git ls-remote origin main | grep -q $(git rev-parse HEAD) && curl -s -o /tmp/asaad-live.html -w "%{http_code}" https://neoasaad.github.io | grep -q '^200$' && grep -q 'Site coming soon' /tmp/asaad-live.html && ! grep -q '/Asaad_Webpage/' /tmp/asaad-live.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows the Phase 1 commit (`feat(01):` prefix)
    - `git ls-remote origin main` returns the same SHA as `git rev-parse HEAD` (push succeeded)
    - GitHub Actions run for `deploy.yml` triggered by this push has `conclusion: success` (verified via `gh run list` or user confirmation in checkpoint)
    - `curl -s -o /tmp/asaad-live.html -w "%{http_code}" https://neoasaad.github.io` returns `200`
    - `grep -c 'Site coming soon' /tmp/asaad-live.html` returns at least `1`
    - `grep -c '/Asaad_Webpage/' /tmp/asaad-live.html` returns `0` (no base-path leakage; confirms RESEARCH.md Pitfall #4 avoided)
    - All asset URLs harvested from the live HTML return HTTP 200 or 304 when HEAD-requested (no 404s)
    - `gh run list --workflow=deploy.yml --limit 1` (or user confirmation) shows the latest run as `success`
  </acceptance_criteria>
  <done>Phase 1 commit pushed to `main`, GitHub Actions deploy run finished successfully, https://neoasaad.github.io returns 200 with the placeholder page, no asset 404s, no `/Asaad_Webpage/` paths anywhere in the live HTML. SITE-01 fully satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verifies live site in browser</name>
  <what-built>Phase 1 is now live. The placeholder page proves: Astro 6 scaffold compiles, GitHub Actions workflow runs, GitHub Pages serves the static output at the user-page root URL, and no `/Asaad_Webpage/` base-path bug.</what-built>
  <how-to-verify>
    Final human verification — open in a real browser and confirm:

    1. Visit https://neoasaad.github.io in a fresh browser tab (or hard-refresh: Cmd+Shift+R / Ctrl+Shift+R)
    2. Confirm the page shows:
       - Heading: "Site coming soon."
       - Body line: "Deployment pipeline is live."
    3. Open DevTools → Network tab → reload the page → confirm:
       - Document request returns 200
       - All asset requests (CSS, JS, fonts) return 200 (NOT 404)
       - No request URL contains `/Asaad_Webpage/`
    4. Open DevTools → Console → confirm zero red errors
    5. Visit https://github.com/neoasaad/neoasaad.github.io/actions → confirm the latest workflow run is green

    Reply with:
    - `approved` — everything looks correct, Phase 1 is done
    - any other comment — describes what's wrong (e.g., "asset 404 on /styles.css", "page shows 404", "console error X")
  </how-to-verify>
  <acceptance_criteria>
    - User confirms the live page renders the placeholder text
    - User confirms zero 404 responses in DevTools Network tab
    - User confirms no `/Asaad_Webpage/` paths in any request
    - User confirms zero console errors
    - User confirms latest GitHub Actions run is green
  </acceptance_criteria>
  <resume-signal>Type "approved" if the live site checks out, or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
End-to-end Phase 1 success requires ALL of:
1. `git ls-remote origin main` matches local HEAD SHA
2. Latest workflow run on https://github.com/neoasaad/neoasaad.github.io/actions is green
3. `curl https://neoasaad.github.io` returns 200 with `Site coming soon.` in body
4. Zero asset 404s when loading the live page in a browser
5. Zero `/Asaad_Webpage/` substrings in the live HTML or any asset URL
6. User-confirmed visual verification (Task 4 checkpoint)
</verification>

<success_criteria>
- All four ROADMAP Phase 1 success criteria are TRUE:
  1. `pnpm build` produces `dist/` locally with no errors (verified in Plan 01)
  2. Pushing to `main` triggers the workflow and deploy completes successfully (this plan)
  3. Live URL https://neoasaad.github.io loads the placeholder page with no 404 asset errors (this plan)
  4. `astro.config.mjs` has correct `site`, `output: "static"`, and **NO `base` property** (per CLAUDE.md authoritative — ROADMAP item #4's reference to `base: /Asaad_Webpage` is the stale doc artifact called out by the planner; it is intentionally NOT implemented)
- SITE-01 closed
- Phase 1 ready to be marked complete in STATE.md / ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-deployment-pipeline/01-03-SUMMARY.md` documenting:
- Git remote configured (URL)
- Commit SHA pushed to main
- Workflow run ID + duration + result (link to Actions tab)
- Live URL response code + content sample
- Asset URL audit result (count of assets, all 200)
- Confirmation that `/Asaad_Webpage/` does NOT appear in live HTML (Pitfall #4 avoided)
- Open todo: update ROADMAP.md success criterion #4 wording in a future docs commit (it still references `base: /Asaad_Webpage` — keep tracked but out of scope for this plan)
</output>
