---
phase: 02-static-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
autonomous: true
requirements: [SITE-04]
must_haves:
  truths:
    - "global.css :root declares the seven Phase 2 color tokens"
    - "global.css :root declares the seven Phase 2 spacing tokens"
    - "html element has scroll-behavior: smooth"
    - "html element has scroll-padding-top: 60px"
    - "Existing Phase 1 typography tokens (--font-body, --font-display) remain intact"
    - "pnpm build still succeeds with no errors"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Color, spacing tokens; scroll behavior on html"
      contains: "--color-bg, --color-bg-deep, --color-surface, --color-text, --color-text-muted, --color-accent, --color-accent-hover, --space-xs, --space-sm, --space-md, --space-lg, --space-xl, --space-2xl, --space-3xl, scroll-behavior, scroll-padding-top"
  key_links:
    - from: "src/styles/global.css"
      to: "all Phase 2 components (Nav, Hero, Journey, Footer)"
      via: "CSS custom properties inherited from :root"
      pattern: "var\\(--color-|var\\(--space-"
    - from: "html { scroll-padding-top: 60px }"
      to: "60px sticky nav height"
      via: "anchor jump offset compensation"
      pattern: "scroll-padding-top:\\s*60px"
---

<objective>
Extend `src/styles/global.css` with the Phase 2 design tokens (colors and spacing) and scroll behavior rules. This is Wave 1 foundation work — every Phase 2 component (Nav, Hero, Journey, Footer) consumes these tokens via `var(--color-*)` and `var(--space-*)`, and the smooth scrolling + scroll-padding-top rules are required by SITE-04 and the 60px sticky nav offset.

Purpose: Establish the global token foundation BEFORE any component is written, so components can reference tokens without forward references.
Output: Extended `src/styles/global.css` with `:root` block expanded and `html` rule extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-static-sections/02-CONTEXT.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@.planning/phases/02-static-sections/02-UI-SPEC.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-PATTERNS.md
@CLAUDE.md
@src/styles/global.css

<interfaces>
<!-- Existing Phase 1 token shell (DO NOT REMOVE these). Source: src/styles/global.css -->
:root {
  --font-body: var(--font-inter), system-ui, sans-serif;
  --font-display: var(--font-playfair), Georgia, serif;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html {
  font-family: var(--font-body);
  font-size: 16px;
  line-height: 1.5;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Phase 2 color and spacing tokens to global.css and extend html rule</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (current Phase 1 token shell — must preserve --font-body, --font-display, universal box-sizing reset, and existing html rule)
    - .planning/phases/02-static-sections/02-UI-SPEC.md (CSS Token Additions section — exact hex values and spacing values; binding source)
    - .planning/phases/02-static-sections/02-RESEARCH.md (Pattern 3 — Global CSS Token Extension)
  </read_first>
  <action>
    Edit `src/styles/global.css` to extend it as follows. Do NOT delete or reorder existing Phase 1 declarations.

    1) Inside the existing `:root { ... }` block, AFTER the existing `--font-display` line, add these two grouped sections (preserve comment style for readability):

    ```css
    /* === Phase 2: Colors === */
    --color-bg: #0d1b2a;
    --color-bg-deep: #070d14;
    --color-surface: #1a2a3a;
    --color-text: #f5f5f5;
    --color-text-muted: #9baab8;
    --color-accent: #d4a017;
    --color-accent-hover: #e8b520;

    /* === Phase 2: Spacing (8-point scale) === */
    --space-xs: 4px;
    --space-sm: 8px;
    --space-md: 16px;
    --space-lg: 24px;
    --space-xl: 32px;
    --space-2xl: 48px;
    --space-3xl: 64px;
    ```

    2) Extend the existing `html { ... }` rule by adding two declarations AFTER the existing `line-height: 1.5;` line. The new declarations are:

    ```css
    scroll-behavior: smooth;
    scroll-padding-top: 60px;
    ```

    The final html rule must read (in order):
    ```css
    html {
      font-family: var(--font-body);
      font-size: 16px;
      line-height: 1.5;
      scroll-behavior: smooth;
      scroll-padding-top: 60px;
    }
    ```

    3) Do NOT add the `--space-4xl: 96px` token at the global level — UI-SPEC declares 96px as a hero-only inline value, not a global token.

    4) Do NOT modify the universal `*, *::before, *::after` reset block.

    5) Do NOT import this file from any new place — it is already imported in `src/layouts/Layout.astro` and that import remains the single source.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; grep -q -- "--color-bg: #0d1b2a" src/styles/global.css &amp;&amp; grep -q -- "--color-accent: #d4a017" src/styles/global.css &amp;&amp; grep -q -- "--space-3xl: 64px" src/styles/global.css &amp;&amp; grep -q "scroll-behavior: smooth" src/styles/global.css &amp;&amp; grep -q "scroll-padding-top: 60px" src/styles/global.css &amp;&amp; grep -q -- "--font-body: var(--font-inter)" src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 with no errors
    - `src/styles/global.css` contains literal `--color-bg: #0d1b2a;`
    - `src/styles/global.css` contains literal `--color-bg-deep: #070d14;`
    - `src/styles/global.css` contains literal `--color-surface: #1a2a3a;`
    - `src/styles/global.css` contains literal `--color-text: #f5f5f5;`
    - `src/styles/global.css` contains literal `--color-text-muted: #9baab8;`
    - `src/styles/global.css` contains literal `--color-accent: #d4a017;`
    - `src/styles/global.css` contains literal `--color-accent-hover: #e8b520;`
    - `src/styles/global.css` contains literal `--space-xs: 4px;`
    - `src/styles/global.css` contains literal `--space-sm: 8px;`
    - `src/styles/global.css` contains literal `--space-md: 16px;`
    - `src/styles/global.css` contains literal `--space-lg: 24px;`
    - `src/styles/global.css` contains literal `--space-xl: 32px;`
    - `src/styles/global.css` contains literal `--space-2xl: 48px;`
    - `src/styles/global.css` contains literal `--space-3xl: 64px;`
    - `src/styles/global.css` contains literal `scroll-behavior: smooth;`
    - `src/styles/global.css` contains literal `scroll-padding-top: 60px;`
    - `src/styles/global.css` still contains literal `--font-body: var(--font-inter), system-ui, sans-serif;`
    - `src/styles/global.css` still contains literal `--font-display: var(--font-playfair), Georgia, serif;`
    - `grep -c "scroll-padding-top: 60px" src/styles/global.css` returns `1` (declared exactly once)
  </acceptance_criteria>
  <done>All 14 tokens (7 colors + 7 spacing) present in `:root`; html rule extended with both scroll properties; Phase 1 declarations untouched; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → static asset | global.css is compiled into static CSS bundles served from GitHub Pages CDN; no runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | global.css build output | accept | Static CSS file, no user input, served via HTTPS GitHub Pages CDN — no tamper vector inside scope of Phase 2. |
| T-02-02 | Information Disclosure | CSS custom properties | accept | All values are public design tokens (colors, spacing). No secrets in CSS. |

Phase 2 is purely static visual content. No authentication, no input, no PII handling. ASVS L1 categories V2/V3/V4/V5/V6 do not apply (per 02-RESEARCH.md Security Domain section).
</threat_model>

<verification>
- `pnpm build` exits 0 (build-level smoke test).
- `grep` invariants over `src/styles/global.css` confirm all 14 new tokens and both new html declarations are literally present.
- Phase 1 typography tokens remain (regression guard).
</verification>

<success_criteria>
All tokens declared exactly as specified in 02-UI-SPEC.md "CSS Token Additions" section. The html element gains `scroll-behavior: smooth` and `scroll-padding-top: 60px`. Build remains green. No new files. No imports added or removed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-01-SUMMARY.md` documenting:
- Tokens added (list).
- Confirmation Phase 1 tokens preserved.
- `pnpm build` result.
- Any deviation from UI-SPEC token values (should be none).
</output>
