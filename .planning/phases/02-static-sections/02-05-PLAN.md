---
phase: 02-static-sections
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/layouts/Layout.astro
  - src/pages/index.astro
autonomous: false
requirements: [SITE-04, SITE-05, HERO-01, HERO-02, JOUR-01, JOUR-02, JOUR-03]
must_haves:
  truths:
    - "Layout.astro imports Nav and Footer from ../components/"
    - "Layout.astro renders <Nav /> outside <main> (above slot) and <Footer /> outside <main> (below slot)"
    - "Layout.astro <main> wraps the <slot /> for landmark accessibility"
    - "index.astro imports Hero and Journey from ../components/"
    - "index.astro composes <Hero /> followed by <Journey /> inside <Layout>"
    - "Page title is 'Asaad — Data & AI Professional' (from Pattern 7)"
    - "pnpm build succeeds"
    - "pnpm preview serves the page locally"
    - "Visual verification at 375px shows: sticky nav, full-viewport hero, smooth-scrolling timeline, footer; no horizontal overflow"
    - "Visual verification at 1024px shows: hero + tagline + About above the fold; timeline with alternating left/right entries; sticky nav remains pinned during scroll"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "Updated layout shell with Nav above slot, <main> landmark wrapping slot, Footer below slot"
      contains: "import Nav from \"../components/Nav.astro\", import Footer from \"../components/Footer.astro\", <Nav />, <main>, <Footer />"
    - path: "src/pages/index.astro"
      provides: "Page composing Hero + Journey inside Layout (Phase 2 final page)"
      contains: "import Hero from \"../components/Hero.astro\", import Journey from \"../components/Journey.astro\", <Hero />, <Journey />"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "src/components/Nav.astro and src/components/Footer.astro"
      via: "ES imports + component invocation"
      pattern: "import Nav from \"\\.\\./components/Nav\\.astro\"|import Footer from \"\\.\\./components/Footer\\.astro\""
    - from: "src/pages/index.astro"
      to: "src/components/Hero.astro and src/components/Journey.astro"
      via: "ES imports + component invocation inside <Layout>"
      pattern: "import Hero from \"\\.\\./components/Hero\\.astro\"|import Journey from \"\\.\\./components/Journey\\.astro\""
    - from: "Nav About link (#hero)"
      to: "Hero section id=\"hero\""
      via: "fragment anchor + scroll-behavior: smooth + scroll-padding-top: 60px"
      pattern: "href=\"#hero\""
    - from: "Nav Journey link (#journey)"
      to: "Journey section id=\"journey\""
      via: "fragment anchor + scroll-behavior: smooth + scroll-padding-top: 60px"
      pattern: "href=\"#journey\""
---

<objective>
Wire all Phase 2 components into the page. Update `src/layouts/Layout.astro` to import and render `<Nav />` (above the slot) and `<Footer />` (below the slot), with `<main>` wrapping `<slot />` for landmark accessibility. Rewrite `src/pages/index.astro` to import and compose `<Hero />` and `<Journey />` inside `<Layout>`. Then verify the full page builds, serves via `pnpm preview`, and meets all five Phase 2 success criteria via a human-verify checkpoint at 375px and 1024px.

Purpose: This is the integration plan — components from Wave 2 (Plans 02-02, 02-03, 02-04) plus the tokens from Wave 1 (Plan 02-01) come together as a working page. Closes all seven Phase 2 requirement IDs once verified.
Output: Updated `Layout.astro` and rewritten `index.astro`. After this plan: live URL renders Phase 2 page on next push to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-static-sections/02-CONTEXT.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@.planning/phases/02-static-sections/02-UI-SPEC.md
@.planning/phases/02-static-sections/02-VALIDATION.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-PATTERNS.md
@CLAUDE.md
@src/layouts/Layout.astro
@src/pages/index.astro
@src/components/Nav.astro
@src/components/Hero.astro
@src/components/Journey.astro
@src/components/Footer.astro
@src/styles/global.css

<interfaces>
<!-- Existing Layout.astro (Phase 1). Source: src/layouts/Layout.astro -->
---
import { Font } from "astro:assets";
import "../styles/global.css";

interface Props {
  title?: string;
}
const { title = "Asaad" } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <Font cssVariable="--font-inter" />
    <Font cssVariable="--font-playfair" />
  </head>
  <body>
    <slot />
  </body>
</html>

<!-- Existing index.astro (Phase 1 placeholder — to be replaced). -->
---
import Layout from "../layouts/Layout.astro";
---
<Layout title="Asaad — Coming Soon">
  <main>
    <h1>Site coming soon.</h1>
    <p>Deployment pipeline is live.</p>
  </main>
</Layout>

<!-- Components ready to wire (created in Plans 02-02..02-04): -->
src/components/Nav.astro       — exports default <Nav /> (sticky header, owns no anchors)
src/components/Hero.astro      — exports default <Hero /> (owns #hero anchor)
src/components/Journey.astro   — exports default <Journey /> (owns #journey anchor)
src/components/Footer.astro    — exports default <Footer /> (copyright only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update Layout.astro to import Nav and Footer; wrap slot in <main></name>
  <files>src/layouts/Layout.astro</files>
  <read_first>
    - src/layouts/Layout.astro (current Phase 1 shell — must preserve Font API and global.css imports)
    - src/components/Nav.astro (confirm file exists — created in Plan 02-02)
    - src/components/Footer.astro (confirm file exists — created in Plan 02-03)
    - .planning/phases/02-static-sections/02-RESEARCH.md (Pattern 1 — Extending Layout.astro)
    - .planning/phases/01-foundation-and-deployment-pipeline/01-PATTERNS.md (Layout.astro pattern — preserve Font API import and global.css import)
  </read_first>
  <action>
    Replace the contents of `src/layouts/Layout.astro` with the following (the only differences from the current Phase 1 file are: two new imports, `<Nav />` before `<main>`, `<main>` wrapping `<slot />`, `<Footer />` after `</main>`).

    Implementation rules:
    - Preserve `import { Font } from "astro:assets";` and `import "../styles/global.css";` exactly — these were Phase 1 patterns and the Font API setup must not change.
    - Preserve the `interface Props { title?: string; }` and the destructured default `title = "Asaad"`.
    - Add imports for Nav and Footer with the `.astro` extension.
    - Place `<Nav />` BEFORE `<main>` (outside the main landmark — nav is its own landmark).
    - Wrap `<slot />` in `<main>` to satisfy the `<main>` landmark requirement (Accessibility Contract).
    - Place `<Footer />` AFTER `</main>` (outside the main landmark — footer is its own landmark).
    - Do NOT add `client:*` directives.

    Exact file contents to write:

    ```astro
    ---
    // src/layouts/Layout.astro
    // Phase 2: extended with Nav (above slot) and Footer (below slot). <main> wraps <slot />.
    // Source: https://docs.astro.build/en/basics/layouts.mdx
    import { Font } from "astro:assets";
    import Nav from "../components/Nav.astro";
    import Footer from "../components/Footer.astro";
    import "../styles/global.css";

    interface Props {
      title?: string;
    }

    const { title = "Asaad" } = Astro.props;
    ---
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>{title}</title>
        <Font cssVariable="--font-inter" />
        <Font cssVariable="--font-playfair" />
      </head>
      <body>
        <Nav />
        <main>
          <slot />
        </main>
        <Footer />
      </body>
    </html>
    ```
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; grep -q 'import Nav from "../components/Nav.astro"' src/layouts/Layout.astro &amp;&amp; grep -q 'import Footer from "../components/Footer.astro"' src/layouts/Layout.astro &amp;&amp; grep -q '<Nav />' src/layouts/Layout.astro &amp;&amp; grep -q '<main>' src/layouts/Layout.astro &amp;&amp; grep -q '<slot />' src/layouts/Layout.astro &amp;&amp; grep -q '</main>' src/layouts/Layout.astro &amp;&amp; grep -q '<Footer />' src/layouts/Layout.astro &amp;&amp; grep -q 'import { Font } from "astro:assets"' src/layouts/Layout.astro &amp;&amp; grep -q 'import "../styles/global.css"' src/layouts/Layout.astro &amp;&amp; ! grep -q 'client:' src/layouts/Layout.astro</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `src/layouts/Layout.astro` contains literal `import Nav from "../components/Nav.astro";`
    - `src/layouts/Layout.astro` contains literal `import Footer from "../components/Footer.astro";`
    - `src/layouts/Layout.astro` contains literal `<Nav />`
    - `src/layouts/Layout.astro` contains literal `<main>` and literal `</main>`
    - `src/layouts/Layout.astro` contains literal `<slot />` inside the `<main>` block
    - `src/layouts/Layout.astro` contains literal `<Footer />`
    - `src/layouts/Layout.astro` still contains literal `import { Font } from "astro:assets";` (Font API preserved)
    - `src/layouts/Layout.astro` still contains literal `import "../styles/global.css";` (global tokens preserved)
    - `src/layouts/Layout.astro` still contains `<Font cssVariable="--font-inter" />` and `<Font cssVariable="--font-playfair" />`
    - `src/layouts/Layout.astro` does NOT contain any `client:` directive
    - Order in `<body>` is exactly: `<Nav />`, `<main>...<slot />...</main>`, `<Footer />`
  </acceptance_criteria>
  <done>Layout.astro renders Nav, then a `<main>` containing the page slot, then Footer. Font API and global.css imports preserved. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite index.astro to compose Hero and Journey inside Layout</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current Phase 1 placeholder — fully replace)
    - src/components/Hero.astro (confirm file exists — created in Plan 02-03)
    - src/components/Journey.astro (confirm file exists — created in Plan 02-04)
    - .planning/phases/02-static-sections/02-RESEARCH.md (Pattern 7 — index.astro Rewrite)
    - .planning/phases/02-static-sections/02-UI-SPEC.md (Hero and Journey component contracts)
  </read_first>
  <action>
    Replace the contents of `src/pages/index.astro` entirely. Phase 1 placeholder is removed.

    Implementation rules:
    - Import Layout, Hero, and Journey using relative paths with `.astro` extension.
    - Page title prop: `"Asaad — Data & AI Professional"` (Pattern 7 in 02-RESEARCH.md). The em-dash is a Unicode `—` (U+2014), not two hyphens.
    - Compose `<Hero />` first, then `<Journey />` (chronological page reading order: identity → career narrative).
    - Do NOT import or render `<Nav />` or `<Footer />` here — Layout already does that.
    - Do NOT add a placeholder `<main>` here — Layout already wraps the slot in `<main>`.
    - Do NOT add `client:*` directives.

    Exact file contents to write:

    ```astro
    ---
    // src/pages/index.astro
    // Phase 2: composes Hero and Journey inside Layout. Replaces the Phase 1 placeholder.
    // Nav and Footer are rendered by Layout — do not import here.
    import Layout from "../layouts/Layout.astro";
    import Hero from "../components/Hero.astro";
    import Journey from "../components/Journey.astro";
    ---
    <Layout title="Asaad — Data & AI Professional">
      <Hero />
      <Journey />
    </Layout>
    ```
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; grep -q 'import Layout from "../layouts/Layout.astro"' src/pages/index.astro &amp;&amp; grep -q 'import Hero from "../components/Hero.astro"' src/pages/index.astro &amp;&amp; grep -q 'import Journey from "../components/Journey.astro"' src/pages/index.astro &amp;&amp; grep -q '<Layout title="Asaad — Data &amp; AI Professional">' src/pages/index.astro &amp;&amp; grep -q '<Hero />' src/pages/index.astro &amp;&amp; grep -q '<Journey />' src/pages/index.astro &amp;&amp; ! grep -q 'Coming Soon' src/pages/index.astro &amp;&amp; ! grep -q 'client:' src/pages/index.astro &amp;&amp; ! grep -q '<Nav' src/pages/index.astro &amp;&amp; ! grep -q '<Footer' src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `src/pages/index.astro` contains literal `import Layout from "../layouts/Layout.astro";`
    - `src/pages/index.astro` contains literal `import Hero from "../components/Hero.astro";`
    - `src/pages/index.astro` contains literal `import Journey from "../components/Journey.astro";`
    - `src/pages/index.astro` contains literal `<Layout title="Asaad — Data & AI Professional">` (em-dash, ampersand)
    - `src/pages/index.astro` contains literal `<Hero />`
    - `src/pages/index.astro` contains literal `<Journey />`
    - `<Hero />` appears BEFORE `<Journey />` in the file
    - File does NOT contain literal `Coming Soon` (Phase 1 placeholder removed)
    - File does NOT contain `<Nav` or `<Footer` (those belong to Layout)
    - File does NOT contain any `client:` directive
  </acceptance_criteria>
  <done>index.astro renders <Layout> containing <Hero /> followed by <Journey />. Phase 1 placeholder fully replaced. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build smoke test and preview server warm-up — confirm full page renders without runtime errors</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/02-static-sections/02-VALIDATION.md (sampling rate and post-wave checks)
  </read_first>
  <action>
    Run the full validation pipeline before the human-verify checkpoint:

    1. From repo root, run `pnpm build` — must exit 0 with no errors.
    2. Confirm the build output contains `dist/index.html` and the file is non-empty: `test -s dist/index.html`.
    3. Confirm the built HTML actually contains the four anchors and copy that downstream visual checks depend on:
       - `grep -q 'id="hero"' dist/index.html`
       - `grep -q 'id="journey"' dist/index.html`
       - `grep -q '>Asaad<' dist/index.html` (h1 content rendered)
       - `grep -q '© 2026 Asaad' dist/index.html` (footer rendered)
       - `grep -q 'aria-label="Primary navigation"' dist/index.html` (nav landmark rendered)
    4. Start `pnpm preview` in a background process so the human-verify checkpoint can open it. Do NOT block the task on the preview process.

    If any of the build greps fail, STOP — do not proceed to the human-verify checkpoint. Report failure with the offending grep so the executor can debug Plan 02-01..02-04 outputs.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; test -s dist/index.html &amp;&amp; grep -q 'id="hero"' dist/index.html &amp;&amp; grep -q 'id="journey"' dist/index.html &amp;&amp; grep -q '>Asaad<' dist/index.html &amp;&amp; grep -q '© 2026 Asaad' dist/index.html &amp;&amp; grep -q 'aria-label="Primary navigation"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `dist/index.html` exists and is non-empty
    - `dist/index.html` contains literal `id="hero"`
    - `dist/index.html` contains literal `id="journey"`
    - `dist/index.html` contains the rendered h1 text `Asaad`
    - `dist/index.html` contains literal `© 2026 Asaad`
    - `dist/index.html` contains literal `aria-label="Primary navigation"`
    - `pnpm preview` starts successfully (port 4321 listening) — confirmed by the executor before the next task
  </acceptance_criteria>
  <done>Full page builds without errors and the served HTML contains all five required landmarks/copy. Preview is running on http://localhost:4321 ready for visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — Phase 2 success criteria at 375px and 1024px</name>
  <what-built>
    Wave 1 added color/spacing tokens and scroll behavior to global.css.
    Wave 2 created four components: Nav, Hero, Journey, Footer.
    Wave 3 wired them into Layout (Nav/Footer outside slot, main wraps slot) and rewrote index.astro to compose Hero + Journey.
    The site is now a single-page scroll with sticky nav, 100vh hero (name + tagline + About narrative), 6-entry timeline, and minimal footer.
  </what-built>
  <how-to-verify>
    Open `http://localhost:4321` (the running `pnpm preview` server). Use Chrome/Edge DevTools responsive mode.

    **Step 1: Desktop verification at 1024px width.**
    1. Resize viewport to 1024px width.
    2. Confirm SC-1 (HERO-01, HERO-02): On initial page load (before any scroll), you can see — without scrolling — Asaad's name (large h1), the tagline ("Data & AI professional, inspirational speaker, and technical career coach."), and at least the start of the About narrative paragraphs. The hero gradient (deep navy to near-black) is visible end-to-end across the viewport.
    3. Confirm sticky nav (SC-3, SITE-05): The nav bar with "Asaad" logo on the left and "ABOUT JOURNEY VALUES CONTACT" links on the right is pinned to the top.
    4. Scroll down. The nav remains pinned at the top throughout the scroll.
    5. Confirm SC-2 (JOUR-01, JOUR-02): The Journey section displays 6 timeline entries arranged with a visible center vertical stem; entries alternate left/right of the stem; each entry shows role (bold), org (gold), date (muted), and an impact statement.
    6. Confirm SC-4 (SITE-04): Click the "JOURNEY" nav link. The page smooth-scrolls (not instant jump) to the Journey section, AND the "Journey" heading is fully visible (NOT hidden behind the 60px nav).
    7. Click the "ABOUT" nav link. The page smooth-scrolls back to the hero, and the h1 is fully visible.
    8. Hover over the "VALUES" and "CONTACT" links. They appear visually disabled (opacity 0.5) and do not respond to clicks.
    9. Tab through the page with the keyboard. The Tab key moves focus through About → Journey → (skips Values, Contact) → page links. Values and Contact must NOT receive focus (tabindex="-1").
    10. Continue scrolling to the bottom. Confirm the footer reads exactly "© 2026 Asaad" on a surface-tinted background.

    **Step 2: Mobile verification at 375px width.**
    1. Switch DevTools to 375px width (iPhone SE or custom).
    2. Confirm no horizontal scrollbar appears on the page (SC-5, JOUR-03).
       - In DevTools console, run: `document.documentElement.scrollWidth` — must equal 375 (or very close, within 1-2px rounding). If it exceeds 375, there is overflow.
    3. The nav still shows all 4 link labels (no hamburger in Phase 2) with reduced padding.
    4. The hero h1 size is smaller (~36px) and centered.
    5. Scroll to Journey: timeline entries are now in a SINGLE LEFT-ALIGNED COLUMN with a left-edge vertical line. No alternating left/right. All 6 entries are stacked vertically and readable.
    6. Footer reads "© 2026 Asaad", centered, on surface background.

    **Step 3: Cross-browser sanity (optional but recommended).**
    1. Open the same `http://localhost:4321` in Safari (if on macOS).
    2. Confirm the nav backdrop-filter (subtle blur) renders (Pitfall 7 mitigation).

    **If any of SC-1 through SC-5 fail, describe the failure in detail (what you saw, at what viewport).**
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 success criteria pass at both 375px and 1024px. Otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard user → page | Tab order must skip dead nav links (Values, Contact) so users are not trapped. |
| viewport (mobile 375px) → layout | Mobile must not produce horizontal overflow that could hide the sticky nav links or break readability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial of Service (accessibility) | Tab order through dead nav links | mitigate | Plan 02-02 applied `tabindex="-1"` and `aria-disabled="true"`; this plan's human-verify confirms tab order skips them. |
| T-02-11 | Denial of Service (mobile usability) | Horizontal overflow at 375px | mitigate | Plan 02-02 (nav padding reduction at 767px) and Plan 02-04 (mobile single-column grid) prevent overflow; human-verify confirms `document.documentElement.scrollWidth === 375`. |
| T-02-12 | Tampering | Static page DOM | accept | Static HTML/CSS only, no JS, no input. Served via HTTPS GitHub Pages CDN. |
</threat_model>

<verification>
- Build smoke test (Task 3) confirms HTML output contains all required anchors and copy.
- Human-verify checkpoint (Task 4) confirms all 5 phase success criteria pass at the two binding breakpoints (375px and 1024px).
- Tab-order verification at 1024px confirms the dead-link DoS threat (T-02-10) is mitigated.
- `scrollWidth === 375` check confirms the mobile overflow threat (T-02-11) is mitigated.
</verification>

<success_criteria>
All five Phase 2 ROADMAP success criteria pass:
1. Visitor sees name, tagline, About above the fold (SC-1 / HERO-01, HERO-02).
2. Visitor scrolls through 6-entry timeline showing role, org, period, impact (SC-2 / JOUR-01, JOUR-02).
3. Sticky nav remains visible during scroll (SC-3 / SITE-05).
4. Nav anchor clicks smooth-scroll to correct section (SC-4 / SITE-04).
5. No horizontal overflow at 375px (SC-5 / JOUR-03 + global responsive constraint).

The autonomous flag is `false` because Task 4 is a `checkpoint:human-verify`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-05-SUMMARY.md` documenting:
- Layout.astro changes (imports added, main landmark added).
- index.astro replacement (placeholder → composed page).
- `pnpm build` result.
- `dist/index.html` grep verification results.
- Human-verify outcome at 1024px and 375px (each of SC-1..SC-5 marked pass/fail with notes).
- Any deviations from UI-SPEC discovered during verification (should be none, or noted with rationale).
- Confirmation that the 7 Phase 2 requirement IDs are now satisfied: HERO-01, HERO-02, JOUR-01, JOUR-02, JOUR-03, SITE-04, SITE-05.
</output>
