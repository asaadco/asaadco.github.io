---
phase: 02-static-sections
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Hero.astro
  - src/components/Footer.astro
autonomous: true
requirements: [HERO-01, HERO-02]
must_haves:
  truths:
    - "Hero.astro renders a full-viewport (min-height: 100vh) section with id='hero'"
    - "Hero displays the name 'Asaad' as <h1> in Playfair Display"
    - "Hero displays the tagline 'Data & AI professional, inspirational speaker, and technical career coach.'"
    - "Hero displays 2 paragraphs of About narrative below the tagline"
    - "Hero uses the linear gradient from --color-bg to --color-bg-deep"
    - "Footer.astro renders a copyright-only minimal footer (© 2026 Asaad)"
    - "Footer uses --color-surface background and --color-text-muted text color"
    - "Both components use scoped <style>, reference tokens via var(), and have no client:* directives"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Full-viewport hero section: name, tagline, About narrative (HERO-01, HERO-02)"
      min_lines: 50
      contains: "id=\"hero\", min-height: 100vh, linear-gradient, clamp(36px, 6vw, 56px), max-width: 640px"
    - path: "src/components/Footer.astro"
      provides: "Minimal copyright footer (D-15)"
      min_lines: 15
      contains: "© 2026 Asaad, var(--color-surface), var(--color-text-muted)"
  key_links:
    - from: "src/components/Hero.astro <h1>"
      to: "src/styles/global.css --font-display alias"
      via: "var(--font-display)"
      pattern: "var\\(--font-display\\)"
    - from: "src/components/Hero.astro section"
      to: "Nav 'About' anchor (#hero)"
      via: "id=\"hero\" anchor target"
      pattern: "id=\"hero\""
    - from: "src/components/Footer.astro background"
      to: "src/styles/global.css --color-surface token"
      via: "var(--color-surface)"
      pattern: "var\\(--color-surface\\)"
---

<objective>
Create two static section components: `src/components/Hero.astro` (full-viewport hero with name, tagline, and About narrative) and `src/components/Footer.astro` (minimal copyright-only footer). Both use Phase 2 tokens from `global.css` and follow the UI-SPEC component contracts. Hero satisfies HERO-01 and HERO-02; Footer satisfies D-15 (locked decision — copyright only).

Purpose: Build the top-of-page identity section (Hero, owns `#hero` anchor) and the bottom-of-page strip (Footer) so Plan 02-05 can wire them into Layout/index.
Output: Two new files: `src/components/Hero.astro` and `src/components/Footer.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-static-sections/02-CONTEXT.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@.planning/phases/02-static-sections/02-UI-SPEC.md
@.planning/phases/01-foundation-and-deployment-pipeline/01-PATTERNS.md
@CLAUDE.md
@src/styles/global.css

<interfaces>
<!-- Tokens available from src/styles/global.css after Plan 02-01 (Wave 1). -->
/* Colors */
--color-bg: #0d1b2a;
--color-bg-deep: #070d14;
--color-surface: #1a2a3a;
--color-text: #f5f5f5;
--color-text-muted: #9baab8;
--color-accent: #d4a017;
--color-accent-hover: #e8b520;

/* Spacing */
--space-xs: 4px; --space-sm: 8px; --space-md: 16px;
--space-lg: 24px; --space-xl: 32px; --space-2xl: 48px; --space-3xl: 64px;

/* Typography aliases (use these, NOT --font-inter / --font-playfair) */
--font-body: var(--font-inter), system-ui, sans-serif;
--font-display: var(--font-playfair), Georgia, serif;

/* From UI-SPEC Copywriting Contract — placeholder text to use verbatim */
Hero name: "Asaad"
Hero tagline: "Data & AI professional, inspirational speaker, and technical career coach."
About paragraph 1: "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation."
About paragraph 2: "Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident."
Footer: "© 2026 Asaad"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Hero.astro — full-viewport section with #hero anchor, name h1, tagline, About narrative, and gradient background</name>
  <files>src/components/Hero.astro</files>
  <read_first>
    - src/styles/global.css (confirm Phase 2 tokens from Plan 02-01 exist before referencing them)
    - .planning/phases/02-static-sections/02-UI-SPEC.md (Hero component contract: 100vh, gradient, clamp typography, paragraph spacing, max-width 640px)
    - .planning/phases/02-static-sections/02-RESEARCH.md (Pattern 2 — Astro Component with Typed Props and Scoped Styles; Code Examples section)
    - .planning/phases/02-static-sections/02-CONTEXT.md (D-02 100vh centered no headshot, D-03 dark gradient, D-04 About folded into hero, D-01 placeholder copy)
  </read_first>
  <action>
    Create `src/components/Hero.astro` with exactly the structure below. The directory `src/components/` may already exist from Plan 02-02 — do not error on it.

    Implementation rules:
    - The `<section>` element MUST have `id="hero"` so the Nav `#hero` anchor lands here.
    - The `<section>` MUST be the element that carries `min-height: 100vh` and the gradient background — DO NOT apply `max-width` to the section (Pitfall 2; backgrounds clip otherwise). Wrap content in an inner `.hero-inner` that takes `max-width: 640px` for the About paragraphs.
    - The name renders as `<h1>` (only `<h1>` on the page — Accessibility Contract).
    - Tagline is a `<p class="hero-tagline">` directly under the `<h1>`.
    - About narrative is exactly 2 `<p class="hero-about">` paragraphs (UI-SPEC Copywriting Contract).
    - Use `var(--font-display)` for h1 and tagline; `var(--font-body)` for About paragraphs.
    - Hero name: `font-size: clamp(36px, 6vw, 56px); line-height: 1.1; font-weight: 700;`.
    - Tagline: `font-size: 28px; font-weight: 700; color: var(--color-text-muted); margin-top: var(--space-md);`.
    - About paragraphs: `font-size: 16px; font-weight: 400; line-height: 1.6; color: var(--color-text); max-width: 640px;` with `var(--space-md)` between paragraphs.
    - Section vertical padding: 96px on desktop (>=768px), `var(--space-3xl)` (64px) on mobile (<768px). Horizontal padding: `var(--space-md)` minimum.
    - Background: `linear-gradient(160deg, var(--color-bg) 0%, var(--color-bg-deep) 100%)`.
    - No `client:*` directive. No images. No headshot.

    Exact file contents to write:

    ```astro
    ---
    // src/components/Hero.astro
    // Phase 2: Full-viewport hero section. Static — no client:* directives.
    // Implements: HERO-01 (name + tagline), HERO-02 (About narrative folded in per D-04).
    // Anchor target: #hero (referenced by Nav "About" link).
    ---
    <section id="hero">
      <div class="hero-inner">
        <h1>Asaad</h1>
        <p class="hero-tagline">Data &amp; AI professional, inspirational speaker, and technical career coach.</p>
        <div class="hero-about">
          <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation.</p>
          <p>Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident.</p>
        </div>
      </div>
    </section>

    <style>
      section {
        min-height: 100vh;
        background: linear-gradient(160deg, var(--color-bg) 0%, var(--color-bg-deep) 100%);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: var(--space-3xl) var(--space-md);
        color: var(--color-text);
      }

      .hero-inner {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        max-width: 640px;
        width: 100%;
      }

      h1 {
        font-family: var(--font-display);
        font-size: clamp(36px, 6vw, 56px);
        font-weight: 700;
        line-height: 1.1;
        color: var(--color-text);
      }

      .hero-tagline {
        font-family: var(--font-display);
        font-size: 28px;
        font-weight: 700;
        line-height: 1.2;
        color: var(--color-text-muted);
        margin-top: var(--space-md);
      }

      .hero-about {
        margin-top: var(--space-xl);
        max-width: 640px;
        margin-left: auto;
        margin-right: auto;
        text-align: left;
      }

      .hero-about p {
        font-family: var(--font-body);
        font-size: 16px;
        font-weight: 400;
        line-height: 1.6;
        color: var(--color-text);
      }

      .hero-about p + p {
        margin-top: var(--space-md);
      }

      @media (min-width: 768px) {
        section {
          padding: 96px var(--space-xl);
        }
      }
    </style>
    ```

    Do NOT modify Layout.astro or index.astro in this task. Do NOT import this component anywhere yet — wiring is Plan 02-05's job.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; test -f src/components/Hero.astro &amp;&amp; grep -q 'id="hero"' src/components/Hero.astro &amp;&amp; grep -q '<h1>Asaad</h1>' src/components/Hero.astro &amp;&amp; grep -q 'min-height: 100vh' src/components/Hero.astro &amp;&amp; grep -q 'linear-gradient(160deg, var(--color-bg) 0%, var(--color-bg-deep) 100%)' src/components/Hero.astro &amp;&amp; grep -q 'clamp(36px, 6vw, 56px)' src/components/Hero.astro &amp;&amp; grep -q 'max-width: 640px' src/components/Hero.astro &amp;&amp; grep -q 'var(--font-display)' src/components/Hero.astro &amp;&amp; grep -q 'var(--font-body)' src/components/Hero.astro &amp;&amp; ! grep -q 'client:' src/components/Hero.astro &amp;&amp; ! grep -q '\-\-font-inter' src/components/Hero.astro &amp;&amp; [ "$(grep -c 'class="hero-about"' src/components/Hero.astro)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Hero.astro` exists
    - `pnpm build` exits 0
    - File contains literal `id="hero"` (Nav anchor target)
    - File contains literal `<h1>Asaad</h1>` (single h1 on page)
    - File contains the literal placeholder tagline `Data &amp; AI professional, inspirational speaker, and technical career coach.`
    - File contains `min-height: 100vh` on the section selector
    - File contains literal `linear-gradient(160deg, var(--color-bg) 0%, var(--color-bg-deep) 100%)`
    - File contains literal `clamp(36px, 6vw, 56px)`
    - File contains literal `max-width: 640px` (About narrative readable line length)
    - File contains literal `var(--font-display)` (used on h1 and tagline)
    - File contains literal `var(--font-body)` (used on About paragraphs)
    - File contains literal `font-size: 28px` (tagline size per UI-SPEC)
    - File contains literal `line-height: 1.6` (About paragraph leading per UI-SPEC)
    - File contains literal `padding: 96px var(--space-xl)` inside the `@media (min-width: 768px)` block
    - File contains exactly two `<p>` elements inside `.hero-about` (verify by counting `<p>` lines inside the `.hero-about` div in the rendered file — total `<p>` count in template is 3: tagline + 2 about paragraphs)
    - File does NOT contain any `client:` directive
    - File does NOT reference `--font-inter` or `--font-playfair` directly
    - `max-width` is NOT applied to the `section` selector (Pitfall 2 — would clip gradient)
  </acceptance_criteria>
  <done>Hero.astro is a complete static section with #hero anchor, h1 name, tagline, two About paragraphs, gradient background, responsive padding (96px desktop / 64px mobile), and uses Phase 2 tokens correctly. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Footer.astro — minimal copyright-only footer (D-15)</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/styles/global.css (confirm Phase 2 tokens from Plan 02-01 exist)
    - .planning/phases/02-static-sections/02-UI-SPEC.md (Footer component contract — surface bg, muted text, padding --space-lg)
    - .planning/phases/02-static-sections/02-CONTEXT.md (D-15 — minimal: "© 2026 Asaad", no social links, no email)
  </read_first>
  <action>
    Create `src/components/Footer.astro`. The directory `src/components/` already exists from prior tasks.

    Implementation rules:
    - Use the semantic `<footer>` landmark element.
    - Content: exactly the string `© 2026 Asaad` (Unicode copyright character `©`, NOT `(c)`). UI-SPEC Copywriting Contract is binding.
    - No social links, no email, no nav, no additional content (D-15).
    - Background: `var(--color-surface)`.
    - Text: `var(--color-text-muted)`, `var(--font-body)`, 14px, weight 400, centered.
    - Padding: `var(--space-lg)` top and bottom.
    - No `client:*` directive.

    Exact file contents to write:

    ```astro
    ---
    // src/components/Footer.astro
    // Phase 2: minimal copyright-only footer. Static — no client:* directives.
    // Implements: D-15 (copyright only — no social links, no email).
    ---
    <footer>
      <p>© 2026 Asaad</p>
    </footer>

    <style>
      footer {
        background-color: var(--color-surface);
        padding: var(--space-lg) var(--space-md);
        text-align: center;
      }

      footer p {
        font-family: var(--font-body);
        font-size: 14px;
        font-weight: 400;
        line-height: 1.4;
        color: var(--color-text-muted);
      }
    </style>
    ```

    Do NOT modify Layout.astro or index.astro in this task. Do NOT import this component anywhere yet.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; test -f src/components/Footer.astro &amp;&amp; grep -q '<footer>' src/components/Footer.astro &amp;&amp; grep -q '© 2026 Asaad' src/components/Footer.astro &amp;&amp; grep -q 'background-color: var(--color-surface)' src/components/Footer.astro &amp;&amp; grep -q 'color: var(--color-text-muted)' src/components/Footer.astro &amp;&amp; grep -q 'var(--font-body)' src/components/Footer.astro &amp;&amp; ! grep -q 'client:' src/components/Footer.astro &amp;&amp; ! grep -qiE '(twitter|linkedin|github|mailto:|social)' src/components/Footer.astro</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Footer.astro` exists
    - `pnpm build` exits 0
    - File contains literal `<footer>` element (semantic landmark)
    - File contains literal `© 2026 Asaad` (Unicode `©`, exact spacing)
    - File contains literal `background-color: var(--color-surface)`
    - File contains literal `color: var(--color-text-muted)`
    - File contains literal `font-size: 14px`
    - File contains literal `var(--font-body)`
    - File contains literal `padding: var(--space-lg) var(--space-md)`
    - File contains literal `text-align: center`
    - File does NOT contain any `client:` directive
    - File does NOT contain any of: `twitter`, `linkedin`, `github`, `mailto:`, `social` (case-insensitive — D-15 minimal scope)
  </acceptance_criteria>
  <done>Footer.astro is a complete minimal footer with copyright text, surface background, muted text color, body font, centered, padded. No extra content. Build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → static asset | Hero and Footer are compiled to static HTML/CSS. No runtime input, no dynamic content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Hero/Footer DOM | accept | Static HTML, no JS, no input. Served via HTTPS GitHub Pages CDN. |
| T-02-07 | Information Disclosure | Hero copy / Footer copyright | accept | All content is intentionally public placeholder text per D-01 / D-15. No PII, no secrets. |
</threat_model>

<verification>
- `pnpm build` confirms both files compile cleanly with the rest of the project (Layout still works in isolation since these components are not yet imported).
- Greps confirm structural and stylistic literals required by HERO-01, HERO-02, and D-15.
- Negative greps confirm no JS hydration and no extraneous content beyond the locked decisions.
</verification>

<success_criteria>
Hero.astro and Footer.astro are complete, self-contained components ready to be wired by Plan 02-05. Hero owns the `#hero` anchor that the Nav "About" link targets. Both components reference Phase 2 tokens correctly and contain the exact placeholder copy from UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-03-SUMMARY.md` documenting:
- Two files created.
- Confirmation Hero owns `#hero` anchor.
- Confirmation tagline + About paragraphs use UI-SPEC Copywriting Contract verbatim.
- Confirmation Footer is copyright-only (no social links).
- `pnpm build` result.
</output>
